version: 2.1

jobs:
  backend:
    docker:
      - image: circleci/node:14

    # working_directory: ~/Backend_NodeJS

    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: cd Backend_NodeJS && npm install
      - run:
          name: Run Tests
          command: cd Backend_NodeJS && npm run test

  frontend:
    docker:
      - image: circleci/node:14

    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: cd.. && cd frontend/revive-gallery-app && npm install
      - run:
          name: Run Tests
          command: cd.. && cd frontend/revive-gallery-app && npm test

  # deploy:
  #   docker:
  #     - image: circleci/node:14

  #   steps:
  #     - attach_workspace:
  #         at: ~/ReviveGallery
  #     - run:
  #         name: Start Backend_NodeJS
  #         command: npm start


workflows:
  version: 2
  build_and_deploy:
    jobs:
      - backend
      - frontend
      # - deploy:
      #     requires:
      #       - backend


